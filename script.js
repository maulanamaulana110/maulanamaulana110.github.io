AOS.init({
  duration: 800,
  once: true,
});

console.log("💖 Welcome to Hilfa’s Cute Portfolio!");

function toggleCard(card) {
  card.classList.toggle("active");
}

function toggleCard(card) {
  card.classList.toggle("active");
}
